import './globals.css';
export const metadata = { title: 'MiddlePayAdmin' };
export default ({ children }: { children: React.ReactNode }) => (
  <html lang="id">
    <body className="bg-slate-950">{children}</body>
  </html>
);